namespace FoxIDs.Client.Models.ViewModels
{
    public enum LogTimeIntervals
    {
        ThirtySeconds = 30,
        OneMinute = 60,
        FiveMinutes = 300,
        FifteenMinutes = 900,
        ThirtyMinutes = 1800,
        OneHour = 3600,
        SixHours = 21600,
        TwelveHours = 43200,
        TwentyfourHours = 86400,
    }
}

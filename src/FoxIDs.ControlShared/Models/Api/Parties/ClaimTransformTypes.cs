namespace FoxIDs.Models.Api
{
    public enum ClaimTransformTypes
    {
        Constant = 10,
        MatchClaim = 17,
        Match = 20,
        RegexMatch = 25,
        Map = 30,
        RegexMap = 35,
        Concatenate = 40
    }
}
